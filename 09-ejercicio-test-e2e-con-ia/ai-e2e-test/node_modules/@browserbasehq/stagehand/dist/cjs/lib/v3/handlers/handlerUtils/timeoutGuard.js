"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createTimeoutGuard = createTimeoutGuard;
const sdkErrors_js_1 = require("../../types/public/sdkErrors.js");
function createTimeoutGuard(timeoutMs, errorFactory) {
    if (!timeoutMs || timeoutMs <= 0) {
        return () => { };
    }
    const startTime = Date.now();
    return () => {
        if (Date.now() - startTime >= timeoutMs) {
            const err = errorFactory?.(timeoutMs) ?? new sdkErrors_js_1.TimeoutError("operation", timeoutMs);
            throw err;
        }
    };
}
//# sourceMappingURL=timeoutGuard.js.map