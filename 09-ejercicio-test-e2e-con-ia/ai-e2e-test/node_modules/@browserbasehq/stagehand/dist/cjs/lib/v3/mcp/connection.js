"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.connectToMCPServer = void 0;
const index_js_1 = require("@modelcontextprotocol/sdk/client/index.js");
const streamableHttp_js_1 = require("@modelcontextprotocol/sdk/client/streamableHttp.js");
const stdio_js_1 = require("@modelcontextprotocol/sdk/client/stdio.js");
const sdkErrors_js_1 = require("../types/public/sdkErrors.js");
const connectToMCPServer = async (serverConfig) => {
    try {
        let transport;
        let clientOptions;
        let requestOptions;
        // Check if it's a stdio config (has 'command' property)
        if (typeof serverConfig === "object" && "command" in serverConfig) {
            transport = new stdio_js_1.StdioClientTransport(serverConfig);
        }
        else {
            // Handle URL-based connection
            let serverUrl;
            if (typeof serverConfig === "string" || serverConfig instanceof URL) {
                serverUrl = serverConfig;
            }
            else {
                serverUrl = serverConfig.serverUrl;
                clientOptions = serverConfig
                    .clientOptions;
                requestOptions = serverConfig
                    .requestOptions;
            }
            transport = new streamableHttp_js_1.StreamableHTTPClientTransport(new URL(serverUrl), requestOptions);
        }
        const client = new index_js_1.Client({
            name: "Stagehand",
            version: "1.0.0",
            ...clientOptions,
        });
        await client.connect(transport);
        try {
            await client.ping();
        }
        catch (pingError) {
            await client.close();
            throw new sdkErrors_js_1.MCPConnectionError(serverConfig.toString(), pingError);
        }
        return client;
    }
    catch (error) {
        // Handle any errors during transport/client creation or connection
        if (error instanceof sdkErrors_js_1.MCPConnectionError) {
            throw error; // Re-throw our custom error
        }
        throw new sdkErrors_js_1.MCPConnectionError(serverConfig.toString(), error);
    }
};
exports.connectToMCPServer = connectToMCPServer;
//# sourceMappingURL=connection.js.map