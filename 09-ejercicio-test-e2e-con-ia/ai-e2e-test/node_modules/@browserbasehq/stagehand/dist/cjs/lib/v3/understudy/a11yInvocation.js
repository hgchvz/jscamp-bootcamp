"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildA11yInvocation = buildA11yInvocation;
const a11yScripts_generated_js_1 = require("../dom/build/a11yScripts.generated.js");
/**
 * Wrap a generated a11y script in a self-invoking expression that first ensures
 * the bootstrap has run, then calls the requested helper via its global ref.
 * This mirrors the locator resolver’s injection path so any CDP Runtime.evaluate
 * can reuse the shared bundle without inlining JS strings.
 */
function buildA11yInvocation(name, args) {
    const invocation = `${a11yScripts_generated_js_1.a11yScriptGlobalRefs[name]}(${args.join(", ")})`;
    return `(() => { ${a11yScripts_generated_js_1.a11yScriptBootstrap}; return ${invocation}; })()`;
}
//# sourceMappingURL=a11yInvocation.js.map