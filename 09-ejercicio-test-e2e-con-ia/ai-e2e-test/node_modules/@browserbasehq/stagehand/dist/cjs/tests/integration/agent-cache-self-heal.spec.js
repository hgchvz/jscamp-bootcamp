"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const test_1 = require("@playwright/test");
const promises_1 = __importDefault(require("fs/promises"));
const path_1 = __importDefault(require("path"));
const v3_js_1 = require("../../lib/v3/v3.js");
const v3_config_js_1 = require("./v3.config.js");
test_1.test.describe("Agent cache self-heal (e2e)", () => {
    let v3;
    let cacheDir;
    // eslint-disable-next-line no-empty-pattern
    test_1.test.beforeEach(async ({}, testInfo) => {
        await promises_1.default.mkdir(testInfo.outputDir, { recursive: true });
        cacheDir = await promises_1.default.mkdtemp(path_1.default.join(testInfo.outputDir, "agent-cache-"));
        v3 = new v3_js_1.V3({
            ...v3_config_js_1.v3TestConfig,
            cacheDir,
            selfHeal: true,
        });
        await v3.init();
    });
    test_1.test.afterEach(async () => {
        await v3?.close?.().catch(() => { });
    });
    (0, test_1.test)("replays heal corrupted selectors", async () => {
        test_1.test.setTimeout(120_000);
        const agent = v3.agent({
            model: "anthropic/claude-haiku-4-5-20251001",
        });
        const page = v3.context.pages()[0];
        const url = "https://browserbase.github.io/stagehand-eval-sites/sites/shadow-dom/";
        const instruction = "click the button";
        await page.goto(url, { waitUntil: "networkidle" });
        const firstResult = await agent.execute({ instruction, maxSteps: 20 });
        (0, test_1.expect)(firstResult.success).toBe(true);
        const cachePath = await locateAgentCacheFile(cacheDir);
        const originalEntry = await readCacheEntry(cachePath);
        const originalActionStep = findFirstActionStep(originalEntry);
        (0, test_1.expect)(originalActionStep).toBeDefined();
        const originalSelector = originalActionStep?.actions?.[0]?.selector;
        (0, test_1.expect)(typeof originalSelector).toBe("string");
        // Corrupt the cached selector so the replay needs to self-heal.
        if (originalActionStep?.actions?.[0]) {
            originalActionStep.actions[0].selector = "xpath=/yeee";
        }
        await promises_1.default.writeFile(cachePath, JSON.stringify(originalEntry, null, 2), "utf8");
        // Second run should replay from cache, self-heal, and update the file.
        await page.goto(url, { waitUntil: "networkidle" });
        const replayResult = await agent.execute({ instruction, maxSteps: 20 });
        (0, test_1.expect)(replayResult.success).toBe(true);
        const healedEntry = await readCacheEntry(cachePath);
        const healedActionStep = findFirstActionStep(healedEntry);
        (0, test_1.expect)(healedActionStep?.actions?.[0]?.selector).toBe(originalSelector);
        (0, test_1.expect)(healedActionStep?.actions?.[0]?.selector).not.toBe("xpath=/yeee");
        (0, test_1.expect)(healedEntry.timestamp).not.toBe(originalEntry.timestamp);
    });
});
async function locateAgentCacheFile(cacheDir) {
    const deadline = Date.now() + 10_000;
    while (Date.now() < deadline) {
        const entries = await promises_1.default.readdir(cacheDir);
        const agentFiles = entries.filter((file) => file.startsWith("agent-"));
        if (agentFiles.length > 0) {
            return path_1.default.join(cacheDir, agentFiles[0]);
        }
        await new Promise((resolve) => setTimeout(resolve, 200));
    }
    throw new Error("Timed out waiting for agent cache entry to be written");
}
async function readCacheEntry(cachePath) {
    const raw = await promises_1.default.readFile(cachePath, "utf8");
    return JSON.parse(raw);
}
function findFirstActionStep(entry) {
    return entry.steps.find((step) => {
        const actions = step.actions;
        return Array.isArray(actions) && actions.length > 0;
    });
}
//# sourceMappingURL=agent-cache-self-heal.spec.js.map