"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const test_1 = require("@playwright/test");
const v3_js_1 = require("../../lib/v3/v3.js");
const v3_dynamic_config_js_1 = require("./v3.dynamic.config.js");
const testUtils_js_1 = require("./testUtils.js");
test_1.test.describe.configure({ mode: "parallel" });
test_1.test.describe("V3 default page tracking", () => {
    let v3;
    test_1.test.beforeEach(async () => {
        v3 = new v3_js_1.V3(v3_dynamic_config_js_1.v3DynamicTestConfig);
        await v3.init();
    });
    test_1.test.afterEach(async () => {
        await (0, testUtils_js_1.closeV3)(v3);
    });
    (0, test_1.test)("activePage points to initial page", async () => {
        const ctx = v3.context;
        // Should have at least one top-level page
        const pages = ctx.pages();
        (0, test_1.expect)(pages.length).toBeGreaterThanOrEqual(1);
        const active = ctx.activePage();
        (0, test_1.expect)(active).toBeTruthy();
        // mainFrameId should be a non-empty string
        (0, test_1.expect)(active.mainFrameId().length).toBeGreaterThan(0);
    });
    (0, test_1.test)("activePage switches to most recent top-level page and reverts on close", async () => {
        const ctx = v3.context;
        const newPage = await ctx.newPage("https://example.com/");
        const activeAfterCreate = await ctx.awaitActivePage();
        (0, test_1.expect)(activeAfterCreate.url()).toContain(newPage.url());
    });
    (0, test_1.test)("popup default-page flow via five-tab site", async () => {
        const ctx = v3.context;
        // 1) Navigate the default page to the site
        const root = await ctx.awaitActivePage();
        await root.goto("https://browserbase.github.io/stagehand-eval-sites/sites/five-tab/", { waitUntil: "load", timeoutMs: 15000 });
        // 2) Click button on the page to open a new tab → page2.html
        await root.locator("xpath=/html/body/button").click();
        const page2 = await ctx.awaitActivePage();
        (0, test_1.expect)(page2.url()).toBe("https://browserbase.github.io/stagehand-eval-sites/sites/five-tab/page2.html");
        // 3) On the default page (now page2), click its button → open page3 popup
        await page2.locator("xpath=/html/body/button").click();
        const page3 = await ctx.awaitActivePage();
        (0, test_1.expect)(page3.url()).toBe("https://browserbase.github.io/stagehand-eval-sites/sites/five-tab/page3.html");
        // 4) Close the current page (page3) and ensure the default page reverts to page2
        await page3.close();
        const backToPage2 = await ctx.awaitActivePage();
        (0, test_1.expect)(backToPage2.url()).toBe("https://browserbase.github.io/stagehand-eval-sites/sites/five-tab/page2.html");
    });
});
//# sourceMappingURL=default-page-tracking.spec.js.map