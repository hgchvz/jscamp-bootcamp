"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const test_1 = require("@playwright/test");
const v3_js_1 = require("../../lib/v3/v3.js");
const sdk_1 = __importDefault(require("@browserbasehq/sdk"));
const adm_zip_1 = __importDefault(require("adm-zip"));
const v3_dynamic_config_js_1 = require("./v3.dynamic.config.js");
const testUtils_js_1 = require("./testUtils.js");
const pdfRe = /sample-(\d{13})+\.pdf/;
test_1.test.describe("downloads on browserbase", () => {
    let v3;
    test_1.test.beforeEach(async () => {
        v3 = new v3_js_1.V3(v3_dynamic_config_js_1.v3DynamicTestConfig);
        await v3.init();
    });
    test_1.test.afterEach(async () => {
        await (0, testUtils_js_1.closeV3)(v3);
    });
    (0, test_1.test)("downloaded pdf is available via downloads api", async () => {
        const browserTarget = (process.env.STAGEHAND_BROWSER_TARGET ?? "local").toLowerCase();
        const isBrowserbase = browserTarget === "browserbase";
        // Skip this test in LOCAL mode as it requires Browserbase session
        test_1.test.skip(!isBrowserbase, "Skipping Browserbase-only downloads test in LOCAL mode");
        // Skip if BROWSERBASE_API_KEY is not set
        test_1.test.skip(!process.env.BROWSERBASE_API_KEY, "Skipping test: BROWSERBASE_API_KEY not set");
        // Tiny timeout to force the race to hit the timeout branch
        const page = v3.context.pages()[0];
        await page.goto("https://browserbase.github.io/stagehand-eval-sites/sites/download-on-click/");
        await page.locator("/html/body/button").click();
        await (0, test_1.expect)(async () => {
            const bb = new sdk_1.default();
            const zipBuffer = await bb.sessions.downloads.list(v3.browserbaseSessionId);
            if (!zipBuffer) {
                throw new Error(`Download buffer is empty for session ${v3.browserbaseSessionId}`);
            }
            const zip = new adm_zip_1.default(Buffer.from(await zipBuffer.arrayBuffer()));
            const zipEntries = zip.getEntries();
            const pdfEntry = zipEntries.find((entry) => pdfRe.test(entry.entryName));
            if (!pdfEntry) {
                throw new Error(`Session ${v3.browserbaseSessionId} is missing a file matching "${pdfRe.toString()}" in its zip entries: ${JSON.stringify(zipEntries.map((entry) => entry.entryName))}`);
            }
            const expectedFileSize = 13264;
            (0, test_1.expect)(pdfEntry.header.size).toBe(expectedFileSize);
        }).toPass({
            timeout: 30_000,
        });
    });
});
//# sourceMappingURL=downloads.spec.js.map