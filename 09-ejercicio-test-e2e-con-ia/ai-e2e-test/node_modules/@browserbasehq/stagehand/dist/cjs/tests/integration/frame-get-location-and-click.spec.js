"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const test_1 = require("@playwright/test");
const v3_js_1 = require("../../lib/v3/v3.js");
const v3_config_js_1 = require("./v3.config.js");
test_1.test.describe("Coordinate-based clicking", () => {
    let v3;
    test_1.test.beforeEach(async () => {
        v3 = new v3_js_1.V3(v3_config_js_1.v3TestConfig);
        await v3.init();
    });
    test_1.test.afterEach(async () => {
        await v3?.close?.().catch(() => { });
    });
    (0, test_1.test)("clicking by coordinates toggles a button state", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <button id="btn" onclick="this.dataset.clicked = (this.dataset.clicked==='1'?'0':'1')">Click</button>
            <div id="out"></div>
            <script>
              const btn = document.getElementById('btn');
              const out = document.getElementById('out');
              const update = () => { out.textContent = btn.dataset.clicked === '1' ? 'clicked' : 'idle'; };
              update();
              btn.addEventListener('click', update);
            </script>
          </body></html>`));
        // Initial state should be idle
        let state = await page.mainFrame().evaluate(() => {
            const out = document.getElementById("out");
            return out?.textContent || "";
        });
        (0, test_1.expect)(state).toBe("idle");
        // Compute button location via Frame.getLocationForSelector
        const { x, y, width, height } = await page
            .mainFrame()
            .getLocationForSelector("#btn");
        // Click near the center of the button using Page.click coordinates
        const cx = Math.round(x + width / 2);
        const cy = Math.round(y + height / 2);
        await page.click(cx, cy);
        state = await page.mainFrame().evaluate(() => {
            const out = document.getElementById("out");
            return out?.textContent || "";
        });
        (0, test_1.expect)(state).toBe("clicked");
        // Click again to toggle back to idle
        await page.click(cx, cy);
        state = await page.mainFrame().evaluate(() => {
            const out = document.getElementById("out");
            return out?.textContent || "";
        });
        (0, test_1.expect)(state).toBe("idle");
    });
});
//# sourceMappingURL=frame-get-location-and-click.spec.js.map