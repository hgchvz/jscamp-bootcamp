"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const test_1 = require("@playwright/test");
const v3_js_1 = require("../../lib/v3/v3.js");
const v3_config_js_1 = require("./v3.config.js");
test_1.test.describe("Locator content methods (textContent, innerHtml, innerText, inputValue)", () => {
    let v3;
    test_1.test.beforeEach(async () => {
        v3 = new v3_js_1.V3(v3_config_js_1.v3TestConfig);
        await v3.init();
    });
    test_1.test.afterEach(async () => {
        await v3?.close?.().catch((e) => {
            void e;
        });
    });
    (0, test_1.test)("Locator.textContent() returns raw text including hidden content", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <div id="content">
              Hello
              <span style="display:none">Hidden</span>
              World
            </div>
          </body></html>`));
        const content = await page.mainFrame().locator("#content").textContent();
        // textContent includes all text nodes, even hidden ones
        (0, test_1.expect)(content).toContain("Hello");
        (0, test_1.expect)(content).toContain("Hidden");
        (0, test_1.expect)(content).toContain("World");
    });
    (0, test_1.test)("Locator.innerText() returns visible text only", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <div id="content">
              Visible
              <span style="display:none">Hidden</span>
              Text
            </div>
          </body></html>`));
        const text = await page.mainFrame().locator("#content").innerText();
        // innerText is layout-aware and excludes hidden elements
        (0, test_1.expect)(text).toContain("Visible");
        (0, test_1.expect)(text).toContain("Text");
        (0, test_1.expect)(text).not.toContain("Hidden");
    });
    (0, test_1.test)("Locator.innerHtml() returns HTML markup", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <div id="container">
              <p class="para">Hello</p>
              <strong>World</strong>
            </div>
          </body></html>`));
        const html = await page.mainFrame().locator("#container").innerHtml();
        (0, test_1.expect)(html).toContain('<p class="para">Hello</p>');
        (0, test_1.expect)(html).toContain("<strong>World</strong>");
    });
    (0, test_1.test)("Locator.inputValue() reads value from input elements", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <input id="text-input" type="text" value="hello world" />
            <textarea id="textarea">multi
line
text</textarea>
            <input id="number-input" type="number" value="42" />
          </body></html>`));
        const textValue = await page
            .mainFrame()
            .locator("#text-input")
            .inputValue();
        (0, test_1.expect)(textValue).toBe("hello world");
        const taValue = await page.mainFrame().locator("#textarea").inputValue();
        (0, test_1.expect)(taValue).toBe("multi\nline\ntext");
        const numValue = await page
            .mainFrame()
            .locator("#number-input")
            .inputValue();
        (0, test_1.expect)(numValue).toBe("42");
    });
    (0, test_1.test)("Locator.textContent() on empty elements returns empty string", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <div id="empty"></div>
            <span id="whitespace">   </span>
          </body></html>`));
        const empty = await page.mainFrame().locator("#empty").textContent();
        (0, test_1.expect)(empty).toBe("");
        const whitespace = await page
            .mainFrame()
            .locator("#whitespace")
            .textContent();
        (0, test_1.expect)(whitespace.trim()).toBe("");
    });
    (0, test_1.test)("Locator.innerText() with nested elements and formatting", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <div id="formatted">
              <p>Line 1</p>
              <p>Line 2</p>
              <ul>
                <li>Item 1</li>
                <li>Item 2</li>
              </ul>
            </div>
          </body></html>`));
        const text = await page.mainFrame().locator("#formatted").innerText();
        (0, test_1.expect)(text).toContain("Line 1");
        (0, test_1.expect)(text).toContain("Line 2");
        (0, test_1.expect)(text).toContain("Item 1");
        (0, test_1.expect)(text).toContain("Item 2");
    });
    (0, test_1.test)("Locator.inputValue() on contenteditable elements", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <div id="editable" contenteditable="true">Editable content</div>
          </body></html>`));
        const value = await page.mainFrame().locator("#editable").inputValue();
        (0, test_1.expect)(value).toBe("Editable content");
    });
    (0, test_1.test)("Locator.innerHtml() preserves attributes and structure", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <div id="complex">
              <a href="/link" class="link-class">Link</a>
              <img src="image.png" alt="test" />
            </div>
          </body></html>`));
        const html = await page.mainFrame().locator("#complex").innerHtml();
        (0, test_1.expect)(html).toContain('href="/link"');
        (0, test_1.expect)(html).toContain('class="link-class"');
        (0, test_1.expect)(html).toContain('src="image.png"');
        (0, test_1.expect)(html).toContain('alt="test"');
    });
    (0, test_1.test)("Locator.textContent() vs innerText() with script/style tags", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <div id="mixed">
              Visible text
              <script>console.log('script');</script>
              <style>body { color: red; }</style>
              More visible
            </div>
          </body></html>`));
        const textContent = await page.mainFrame().locator("#mixed").textContent();
        // textContent includes script content
        (0, test_1.expect)(textContent).toContain("Visible text");
        (0, test_1.expect)(textContent).toContain("More visible");
        const innerText = await page.mainFrame().locator("#mixed").innerText();
        // innerText excludes script/style
        (0, test_1.expect)(innerText).toContain("Visible text");
        (0, test_1.expect)(innerText).toContain("More visible");
        (0, test_1.expect)(innerText).not.toContain("console.log");
    });
    (0, test_1.test)("Locator.inputValue() returns empty string for non-input elements", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <div id="div">Not an input</div>
            <input id="empty-input" type="text" value="" />
          </body></html>`));
        const divValue = await page.mainFrame().locator("#div").inputValue();
        (0, test_1.expect)(divValue).toBe("");
        const emptyInput = await page
            .mainFrame()
            .locator("#empty-input")
            .inputValue();
        (0, test_1.expect)(emptyInput).toBe("");
    });
});
//# sourceMappingURL=locator-content-methods.spec.js.map