"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const test_1 = require("@playwright/test");
const v3_js_1 = require("../../lib/v3/v3.js");
const v3_dynamic_config_js_1 = require("./v3.dynamic.config.js");
const testUtils_js_1 = require("./testUtils.js");
test_1.test.describe("Locator count() method with iframes", () => {
    let v3;
    test_1.test.beforeEach(async () => {
        v3 = new v3_js_1.V3(v3_dynamic_config_js_1.v3DynamicTestConfig);
        await v3.init();
    });
    test_1.test.afterEach(async () => {
        await (0, testUtils_js_1.closeV3)(v3);
    });
    (0, test_1.test)("count() does not search inside iframes by default", async () => {
        const page = v3.context.pages()[0];
        // Create a page with buttons in main frame and iframe
        await page.goto("data:text/html," +
            encodeURIComponent(`
        <button>Main Frame Button 1</button>
        <button>Main Frame Button 2</button>
        <iframe id="test-iframe" srcdoc="
          <button>Iframe Button 1</button>
          <button>Iframe Button 2</button>
          <button>Iframe Button 3</button>
        "></iframe>
      `));
        // Wait for iframe to load
        await new Promise((resolve) => setTimeout(resolve, 500));
        // Count buttons in main frame only
        const mainFrameCount = await page.mainFrame().locator("button").count();
        (0, test_1.expect)(mainFrameCount).toBe(2); // Should only find buttons in main frame
    });
    (0, test_1.test)("count() works with frameLocator for iframe content", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`
        <button>Main Frame Button</button>
        <iframe id="test-iframe" srcdoc="
          <button>Iframe Button 1</button>
          <button>Iframe Button 2</button>
          <button>Iframe Button 3</button>
        "></iframe>
      `));
        // Wait for iframe to load
        await new Promise((resolve) => setTimeout(resolve, 500));
        // Count buttons in iframe using frameLocator
        const iframeLocator = page.frameLocator("#test-iframe");
        const iframeCount = await iframeLocator.locator("button").count();
        (0, test_1.expect)(iframeCount).toBe(3); // Should find 3 buttons in iframe
    });
    (0, test_1.test)("count() with nested iframes", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`
        <div class="level-0">Main Frame</div>
        <iframe id="frame1" srcdoc="
          <div class='level-1'>Frame 1</div>
          <iframe id='frame2' srcdoc='
            <div class=&quot;level-2&quot;>Frame 2</div>
            <div class=&quot;level-2&quot;>Frame 2</div>
          '></iframe>
        "></iframe>
      `));
        // Wait for all iframes to load
        await new Promise((resolve) => setTimeout(resolve, 800));
        // Count at each level
        const mainCount = await page.mainFrame().locator(".level-0").count();
        (0, test_1.expect)(mainCount).toBe(1);
        const frame1Count = await page
            .frameLocator("#frame1")
            .locator(".level-1")
            .count();
        (0, test_1.expect)(frame1Count).toBe(1);
        const frame2Count = await page
            .frameLocator("#frame1")
            .frameLocator("#frame2")
            .locator(".level-2")
            .count();
        (0, test_1.expect)(frame2Count).toBe(2);
    });
    (0, test_1.test)("count() with same selector in multiple contexts", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`
        <span class="item">Main 1</span>
        <span class="item">Main 2</span>
        <iframe id="frame1" srcdoc="
          <span class='item'>Frame1 Item</span>
        "></iframe>
        <iframe id="frame2" srcdoc="
          <span class='item'>Frame2 Item 1</span>
          <span class='item'>Frame2 Item 2</span>
          <span class='item'>Frame2 Item 3</span>
        "></iframe>
      `));
        // Wait for iframes to load
        await new Promise((resolve) => setTimeout(resolve, 500));
        // Count in each context
        const mainCount = await page.mainFrame().locator(".item").count();
        const frame1Count = await page
            .frameLocator("#frame1")
            .locator(".item")
            .count();
        const frame2Count = await page
            .frameLocator("#frame2")
            .locator(".item")
            .count();
        (0, test_1.expect)(mainCount).toBe(2); // Main frame items only
        (0, test_1.expect)(frame1Count).toBe(1); // Frame 1 items only
        (0, test_1.expect)(frame2Count).toBe(3); // Frame 2 items only
    });
    (0, test_1.test)("count() returns 0 for non-existent iframe", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html,<div>No iframes here</div>");
        try {
            const frameLocator = page.frameLocator("#non-existent");
            await frameLocator.locator("button").count();
            // If we get here, the test should fail
            (0, test_1.expect)(true).toBe(false);
        }
        catch (error) {
            // Expected behavior - frameLocator should throw when iframe doesn't exist
            (0, test_1.expect)(error.message).toContain("Could not find an element for the given xPath(s):");
        }
    });
});
//# sourceMappingURL=locator-count-iframe.spec.js.map