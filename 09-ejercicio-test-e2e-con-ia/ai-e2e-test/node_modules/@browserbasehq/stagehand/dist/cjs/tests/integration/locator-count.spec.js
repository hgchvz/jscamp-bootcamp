"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const test_1 = require("@playwright/test");
const v3_js_1 = require("../../lib/v3/v3.js");
const v3_dynamic_config_js_1 = require("./v3.dynamic.config.js");
const testUtils_js_1 = require("./testUtils.js");
test_1.test.describe("Locator count() method tests", () => {
    let v3;
    test_1.test.beforeEach(async () => {
        v3 = new v3_js_1.V3(v3_dynamic_config_js_1.v3DynamicTestConfig);
        await v3.init();
    });
    test_1.test.afterEach(async () => {
        await (0, testUtils_js_1.closeV3)(v3);
    });
    (0, test_1.test)("count() returns correct number for CSS selectors", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html,<div class='test'>1</div><div class='test'>2</div><div class='test'>3</div><span>4</span>");
        const locator = page.mainFrame().locator(".test");
        const count = await locator.count();
        (0, test_1.expect)(count).toBe(3);
    });
    (0, test_1.test)("count() returns 0 for non-matching selectors", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html,<div>Test</div>");
        const locator = page.mainFrame().locator(".non-existent");
        const count = await locator.count();
        (0, test_1.expect)(count).toBe(0);
    });
    (0, test_1.test)("count() works with XPath selectors", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html,<button>Button 1</button><button>Button 2</button><button>Button 3</button>");
        const locator = page.mainFrame().locator("//button");
        const count = await locator.count();
        (0, test_1.expect)(count).toBe(3);
    });
    (0, test_1.test)("count() works with text selectors", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html,<div>Click me</div><button>Click me</button><span>Don't click me</span>");
        const locator = page.mainFrame().locator("text=Click me");
        const count = await locator.count();
        // Case-insensitive substring match: also matches "Don't click me"
        (0, test_1.expect)(count).toBe(3);
    });
    (0, test_1.test)("count() handles shadow DOM elements", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent('<div id="host"></div>' +
                "<script>" +
                'const host = document.getElementById("host");' +
                'const shadow = host.attachShadow({mode: "open"});' +
                'shadow.innerHTML = "<button>1</button><button>2</button>";' +
                "</script>"), { waitUntil: "load", timeoutMs: 30000 });
        // Wait a bit for shadow DOM to be attached
        await new Promise((resolve) => setTimeout(resolve, 100));
        const locator = page.mainFrame().locator("button");
        const count = await locator.count();
        (0, test_1.expect)(count).toBe(2);
    });
    (0, test_1.test)("count() works with complex CSS selectors", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html,<div class='container'><span class='item'>1</span><span class='item'>2</span></div><div><span class='item'>3</span></div>");
        const locator = page.mainFrame().locator(".container .item");
        const count = await locator.count();
        (0, test_1.expect)(count).toBe(2);
    });
});
//# sourceMappingURL=locator-count.spec.js.map