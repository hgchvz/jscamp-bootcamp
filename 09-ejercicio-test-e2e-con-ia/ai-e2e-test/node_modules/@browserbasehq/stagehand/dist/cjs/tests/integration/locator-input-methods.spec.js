"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const test_1 = require("@playwright/test");
const v3_js_1 = require("../../lib/v3/v3.js");
const v3_config_js_1 = require("./v3.config.js");
test_1.test.describe("Locator input methods (fill, type, hover, isVisible, isChecked)", () => {
    let v3;
    test_1.test.beforeEach(async () => {
        v3 = new v3_js_1.V3(v3_config_js_1.v3TestConfig);
        await v3.init();
    });
    test_1.test.afterEach(async () => {
        await v3?.close?.().catch((e) => {
            void e;
        });
    });
    (0, test_1.test)("Locator.fill() sets input value directly", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <input id="name" type="text" />
            <div id="out"></div>
          </body></html>`));
        const input = page.mainFrame().locator("#name");
        await input.fill("Hello World");
        const value = await input.inputValue();
        (0, test_1.expect)(value).toBe("Hello World");
    });
    (0, test_1.test)("Locator.type() types text character by character", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <input id="search" type="text" />
          </body></html>`));
        const input = page.mainFrame().locator("#search");
        await input.type("test123", { delay: 10 });
        const value = await input.inputValue();
        (0, test_1.expect)(value).toBe("test123");
    });
    (0, test_1.test)("Locator.hover() moves mouse to element center", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <button id="btn" onmouseover="this.dataset.hovered='true'" onmouseout="this.dataset.hovered='false'">Hover Me</button>
          </body></html>`));
        const btn = page.mainFrame().locator("#btn");
        await btn.hover();
        const hovered = await page.mainFrame().evaluate(() => {
            const b = document.getElementById("btn");
            return b?.dataset.hovered === "true";
        });
        (0, test_1.expect)(hovered).toBe(true);
    });
    (0, test_1.test)("Locator.isVisible() returns true for visible elements", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <div id="visible">I am visible</div>
            <div id="hidden" style="display:none">I am hidden</div>
            <div id="invisible" style="visibility:hidden">I am invisible</div>
            <div id="transparent" style="opacity:0">I am transparent</div>
            <div id="zero-size" style="width:0;height:0">Zero size</div>
          </body></html>`));
        const visible = await page.mainFrame().locator("#visible").isVisible();
        (0, test_1.expect)(visible).toBe(true);
        const hidden = await page.mainFrame().locator("#hidden").isVisible();
        (0, test_1.expect)(hidden).toBe(false);
        const invisible = await page.mainFrame().locator("#invisible").isVisible();
        (0, test_1.expect)(invisible).toBe(false);
        const transparent = await page
            .mainFrame()
            .locator("#transparent")
            .isVisible();
        (0, test_1.expect)(transparent).toBe(false);
        const zeroSize = await page.mainFrame().locator("#zero-size").isVisible();
        (0, test_1.expect)(zeroSize).toBe(false);
    });
    (0, test_1.test)("Locator.isChecked() detects checkbox state", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <input id="checked" type="checkbox" checked />
            <input id="unchecked" type="checkbox" />
            <input id="radio-selected" type="radio" name="opt" checked />
            <input id="radio-unselected" type="radio" name="opt" />
          </body></html>`));
        const checked = await page.mainFrame().locator("#checked").isChecked();
        (0, test_1.expect)(checked).toBe(true);
        const unchecked = await page.mainFrame().locator("#unchecked").isChecked();
        (0, test_1.expect)(unchecked).toBe(false);
        const radioSelected = await page
            .mainFrame()
            .locator("#radio-selected")
            .isChecked();
        (0, test_1.expect)(radioSelected).toBe(true);
        const radioUnselected = await page
            .mainFrame()
            .locator("#radio-unselected")
            .isChecked();
        (0, test_1.expect)(radioUnselected).toBe(false);
    });
    (0, test_1.test)("Locator.fill() on textarea", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <textarea id="ta"></textarea>
          </body></html>`));
        const ta = page.mainFrame().locator("#ta");
        await ta.fill("Multi\nline\ntext");
        const value = await ta.inputValue();
        (0, test_1.expect)(value).toBe("Multi\nline\ntext");
    });
    (0, test_1.test)("Locator.fill() clears and sets new value", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`<!doctype html><html><body>
            <input id="inp" type="text" value="initial" />
          </body></html>`));
        const inp = page.mainFrame().locator("#inp");
        let value = await inp.inputValue();
        (0, test_1.expect)(value).toBe("initial");
        await inp.fill("replaced");
        value = await inp.inputValue();
        (0, test_1.expect)(value).toBe("replaced");
    });
});
//# sourceMappingURL=locator-input-methods.spec.js.map