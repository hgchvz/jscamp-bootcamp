"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const test_1 = require("@playwright/test");
const buffer_1 = require("buffer");
const fs_1 = require("fs");
const path_1 = __importDefault(require("path"));
const crypto_1 = __importDefault(require("crypto"));
const v3_js_1 = require("../../lib/v3/v3.js");
const v3_config_js_1 = require("./v3.config.js");
const FILE_UPLOAD_IFRAME_URL = "https://browserbase.github.io/stagehand-eval-sites/sites/file-uploads-iframe/";
const FILE_UPLOAD_V2_URL = "https://browserbase.github.io/stagehand-eval-sites/sites/file-uploads-2/";
const RESUME_INPUT = "#resumeUpload";
const RESUME_SUCCESS = "#resumeSuccess";
const IMAGES_INPUT = "#imagesUpload";
const IMAGES_SUCCESS = "#imagesSuccess";
const AUDIO_INPUT = "#audioUpload";
const AUDIO_SUCCESS = "#audioSuccess";
const IFRAME_UPLOAD_INPUT = "/html/body/div/iframe/html/body/div/div[1]/input";
const IFRAME_SUCCESS = "body > div > iframe >> html > body > div > div:nth-of-type(2)";
test_1.test.describe("tests setInputFiles()", () => {
    let v3;
    const fixtures = [];
    test_1.test.beforeEach(async () => {
        v3 = new v3_js_1.V3(v3_config_js_1.v3TestConfig);
        await v3.init();
    });
    test_1.test.afterEach(async () => {
        await v3?.close?.().catch(() => { });
        await Promise.all(fixtures.splice(0).map((file) => fs_1.promises.unlink(file).catch(() => { })));
    });
    const createFixture = async (namePrefix, contents, ext = ".txt") => {
        const normalizedExt = ext.startsWith(".") ? ext : `.${ext}`;
        const filename = `${namePrefix}-${crypto_1.default.randomBytes(4).toString("hex")}${normalizedExt}`;
        const filePath = path_1.default.resolve(process.cwd(), filename);
        await fs_1.promises.writeFile(filePath, contents, "utf-8");
        fixtures.push(filePath);
        return filePath;
    };
    const expectUploadSuccess = async (page, successSelector, expectedText) => {
        await test_1.expect
            .poll(() => page.evaluate((selector) => {
            const el = document.querySelector(selector);
            if (!el)
                return "";
            const display = window.getComputedStyle(el).display;
            if (display === "none")
                return "";
            return el.textContent ?? "";
        }, successSelector), { message: `wait for success message at ${successSelector}` })
            .toContain(expectedText);
    };
    const getInputFileCount = async (page, inputSelector) => {
        return await page.evaluate((selector) => {
            const el = document.querySelector(selector);
            if (!(el instanceof HTMLInputElement))
                return 0;
            return el.files?.length ?? 0;
        }, inputSelector);
    };
    const expectFileCount = async (page, inputSelector, expected) => {
        await test_1.expect
            .poll(() => getInputFileCount(page, inputSelector), {
            message: `wait for file count on ${inputSelector}`,
        })
            .toBe(expected);
    };
    (0, test_1.test)("deepLocator uploads and validates within iframe", async () => {
        const page = v3.context.pages()[0];
        await page.goto(FILE_UPLOAD_IFRAME_URL);
        const fixture = await createFixture("iframe-upload", "<p>iframe upload</p>", ".txt");
        await page
            .deepLocator(IFRAME_UPLOAD_INPUT)
            .setInputFiles(path_1.default.relative(process.cwd(), fixture));
        const successLocator = page.deepLocator(IFRAME_SUCCESS);
        await test_1.expect
            .poll(async () => (await successLocator.textContent()) ?? "", {
            message: "wait for iframe upload success",
        })
            .toContain("file uploaded successfully");
    });
    (0, test_1.test)("locator uploads resume via relative path string", async () => {
        const page = v3.context.pages()[0];
        await page.goto(FILE_UPLOAD_V2_URL);
        const fixture = await createFixture("resume", "<p>resume</p>", ".pdf");
        await page
            .locator(RESUME_INPUT)
            .setInputFiles(path_1.default.relative(process.cwd(), fixture));
        await expectUploadSuccess(page, RESUME_SUCCESS, "Resume uploaded!");
        await expectFileCount(page, RESUME_INPUT, 1);
    });
    (0, test_1.test)("locator uploads multiple images via absolute paths", async () => {
        const page = v3.context.pages()[0];
        await page.goto(FILE_UPLOAD_V2_URL);
        const first = await createFixture("image-a", "<p>A</p>", ".png");
        const second = await createFixture("image-b", "<p>B</p>", ".jpeg");
        await page.locator(IMAGES_INPUT).setInputFiles([first, second]);
        await expectUploadSuccess(page, IMAGES_SUCCESS, "Images uploaded!");
        await expectFileCount(page, IMAGES_INPUT, 2);
    });
    (0, test_1.test)("locator uploads audio via payload object", async () => {
        const page = v3.context.pages()[0];
        await page.goto(FILE_UPLOAD_V2_URL);
        await page.locator(AUDIO_INPUT).setInputFiles({
            name: "voice-sample.mp3",
            mimeType: "audio/mpeg",
            buffer: buffer_1.Buffer.from("fake audio bytes", "utf-8"),
        });
        await expectUploadSuccess(page, AUDIO_SUCCESS, "Audio file uploaded!");
        await expectFileCount(page, AUDIO_INPUT, 1);
    });
    (0, test_1.test)("locator uploads multiple payload objects to images input", async () => {
        const page = v3.context.pages()[0];
        await page.goto(FILE_UPLOAD_V2_URL);
        await page.locator(IMAGES_INPUT).setInputFiles([
            {
                name: "payload-a.png",
                mimeType: "image/png",
                buffer: buffer_1.Buffer.from("payload-a", "utf-8"),
            },
            {
                name: "payload-b.png",
                mimeType: "image/png",
                buffer: buffer_1.Buffer.from("payload-b", "utf-8"),
            },
        ]);
        await expectUploadSuccess(page, IMAGES_SUCCESS, "Images uploaded!");
        await expectFileCount(page, IMAGES_INPUT, 2);
    });
});
//# sourceMappingURL=setinputfiles.spec.js.map