"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const test_1 = require("@playwright/test");
const v3_js_1 = require("../../lib/v3/v3.js");
const v3_dynamic_config_js_1 = require("./v3.dynamic.config.js");
const testUtils_js_1 = require("./testUtils.js");
test_1.test.describe("Text selector innermost element matching", () => {
    let v3;
    test_1.test.beforeEach(async () => {
        v3 = new v3_js_1.V3(v3_dynamic_config_js_1.v3DynamicTestConfig);
        await v3.init();
    });
    test_1.test.afterEach(async () => {
        await (0, testUtils_js_1.closeV3)(v3);
    });
    (0, test_1.test)("text selector matches only innermost elements", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`
        <div id="outer">
          <span id="middle">
            <button id="inner">Click me</button>
          </span>
        </div>
      `));
        // Only the button should be counted, not the parent elements
        const count = await page.mainFrame().locator("text=Click me").count();
        (0, test_1.expect)(count).toBe(1);
        // Verify it finds the button element specifically
        const session = page.mainFrame().session;
        const { executionContextId } = await session.send("Page.createIsolatedWorld", {
            frameId: page.mainFrame().frameId,
            worldName: "test-world",
        });
        const evalRes = await session.send("Runtime.evaluate", {
            expression: `(() => {
          const candidates = [];
          const iter = document.createNodeIterator(document.documentElement, NodeFilter.SHOW_ELEMENT);
          let n;
          while ((n = iter.nextNode())) {
            const el = n;
            const t = (el.innerText ?? el.textContent ?? '').trim();
            if (t && t.includes("Click me")) {
              candidates.push(el);
            }
          }
          
          // Find innermost
          for (const candidate of candidates) {
            let isInnermost = true;
            for (const other of candidates) {
              if (candidate !== other && candidate.contains(other)) {
                isInnermost = false;
                break;
              }
            }
            if (isInnermost) return candidate.id;
          }
          return null;
        })()`,
            contextId: executionContextId,
            returnByValue: true,
        });
        (0, test_1.expect)(evalRes.result.value).toBe("inner");
    });
    (0, test_1.test)("multiple innermost elements with same text", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`
        <div>
          <button>Submit</button>
          <span>Some other content</span>
          <button>Submit</button>
        </div>
        <div>
          <a href="#">Submit</a>
        </div>
      `));
        // Should find all three innermost elements (2 buttons + 1 link)
        const count = await page.mainFrame().locator("text=Submit").count();
        (0, test_1.expect)(count).toBe(3);
    });
    (0, test_1.test)("nested text with different innermost elements", async () => {
        const page = v3.context.pages()[0];
        await page.goto("data:text/html," +
            encodeURIComponent(`
        <div id="parent">
          Hello <span id="child">World</span>
        </div>
      `));
        // "Hello" is only in the parent div
        const helloCount = await page.mainFrame().locator("text=Hello").count();
        (0, test_1.expect)(helloCount).toBe(1); // Only the div
        // "World" is only in the span
        const worldCount = await page.mainFrame().locator("text=World").count();
        (0, test_1.expect)(worldCount).toBe(1); // Only the span
        // "Hello World" matches only the parent div (as it's the innermost containing both words)
        const bothCount = await page
            .mainFrame()
            .locator("text=Hello World")
            .count();
        (0, test_1.expect)(bothCount).toBe(1); // Only the div
    });
});
//# sourceMappingURL=text-selector-innermost.spec.js.map