"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
const test_1 = require("@playwright/test");
const v3_js_1 = require("../../lib/v3/v3.js");
const v3_config_js_1 = require("./v3.config.js");
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const os = __importStar(require("os"));
test_1.test.describe("userDataDir persistence", () => {
    let v3;
    let testDir;
    test_1.test.beforeEach(() => {
        testDir = fs.mkdtempSync(path.join(os.tmpdir(), "stagehand-userdata-test-"));
    });
    test_1.test.afterEach(async () => {
        await v3?.close?.().catch(() => { });
        if (testDir && fs.existsSync(testDir)) {
            fs.rmSync(testDir, { recursive: true, force: true });
        }
    });
    (0, test_1.test)("Chrome uses the specified userDataDir", async () => {
        const browserTarget = (process.env.STAGEHAND_BROWSER_TARGET ?? "local").toLowerCase();
        const isBrowserbase = browserTarget === "browserbase";
        test_1.test.skip(isBrowserbase, "Requires local Chromium for userDataDir checks");
        v3 = new v3_js_1.V3({
            ...v3_config_js_1.v3TestConfig,
            localBrowserLaunchOptions: {
                ...(v3_config_js_1.v3TestConfig.localBrowserLaunchOptions ?? {}),
                userDataDir: testDir,
                preserveUserDataDir: true,
            },
        });
        await v3.init();
        const page = v3.context.pages()[0];
        await page.goto("about:blank");
        await test_1.expect
            .poll(() => fs.existsSync(path.join(testDir, "Default")), {
            timeout: 10_000,
        })
            .toBe(true);
        (0, test_1.expect)(fs.existsSync(path.join(testDir, "Local State"))).toBe(true);
    });
});
//# sourceMappingURL=user-data-dir.spec.js.map