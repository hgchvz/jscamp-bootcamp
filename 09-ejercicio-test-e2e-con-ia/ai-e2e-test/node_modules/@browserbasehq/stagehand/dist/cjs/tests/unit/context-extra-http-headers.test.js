"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const context_js_1 = require("../../lib/v3/understudy/context.js");
const mockCDPSession_js_1 = require("./helpers/mockCDPSession.js");
const sdkErrors_js_1 = require("../../lib/v3/types/public/sdkErrors.js");
const makeContext = (sessions) => {
    const sessionsById = new Map(sessions.map((session) => [session.id, session]));
    return {
        _sessionInit: new Set(sessions.map((session) => session.id)),
        conn: {
            getSession: (id) => sessionsById.get(id),
        },
        extraHttpHeaders: null,
    };
};
(0, vitest_1.describe)("V3Context.setExtraHTTPHeaders", () => {
    const setExtraHTTPHeaders = context_js_1.V3Context.prototype.setExtraHTTPHeaders;
    (0, vitest_1.it)("sends headers to all sessions", async () => {
        const sessionA = new mockCDPSession_js_1.MockCDPSession({}, "session-a");
        const sessionB = new mockCDPSession_js_1.MockCDPSession({}, "session-b");
        const ctx = makeContext([sessionA, sessionB]);
        await setExtraHTTPHeaders.call(ctx, {
            "x-stagehand-test": "yes",
        });
        for (const session of [sessionA, sessionB]) {
            (0, vitest_1.expect)(session.callsFor("Network.enable").length).toBe(1);
            (0, vitest_1.expect)(session.callsFor("Network.setExtraHTTPHeaders")[0]?.params).toEqual({
                headers: { "x-stagehand-test": "yes" },
            });
        }
    });
    (0, vitest_1.it)("throws a custom error with session failure details", async () => {
        const sessionA = new mockCDPSession_js_1.MockCDPSession({
            "Network.setExtraHTTPHeaders": () => {
                throw new Error("boom");
            },
        }, "session-a");
        const sessionB = new mockCDPSession_js_1.MockCDPSession({}, "session-b");
        const ctx = makeContext([sessionA, sessionB]);
        const promise = setExtraHTTPHeaders.call(ctx, {
            "x-stagehand-test": "yes",
        });
        await (0, vitest_1.expect)(promise).rejects.toBeInstanceOf(sdkErrors_js_1.StagehandSetExtraHTTPHeadersError);
        try {
            await promise;
        }
        catch (error) {
            const err = error;
            (0, vitest_1.expect)(err.failures).toHaveLength(1);
            (0, vitest_1.expect)(err.failures[0]).toContain("session=session-a");
            (0, vitest_1.expect)(err.failures[0]).toContain("boom");
        }
        (0, vitest_1.expect)(sessionA.callsFor("Network.setExtraHTTPHeaders").length).toBe(1);
        (0, vitest_1.expect)(sessionB.callsFor("Network.setExtraHTTPHeaders").length).toBe(1);
    });
});
//# sourceMappingURL=context-extra-http-headers.test.js.map