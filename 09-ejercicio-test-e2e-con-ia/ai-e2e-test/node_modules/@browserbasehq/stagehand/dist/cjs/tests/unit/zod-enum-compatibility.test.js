"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const z3 = __importStar(require("zod/v3"));
const zod_1 = require("zod");
const handlers_js_1 = require("../../lib/v3/types/private/handlers.js");
/**
 * Tests for Zod v3/v4 compatibility with the SupportedUnderstudyAction enum.
 *
 * This test ensures that z.enum() works correctly with both Zod v3 and v4.
 * The key issue is that z.enum() in Zod v3 does NOT accept TypeScript enums directly -
 * it only accepts string literal tuples. For TypeScript enums, you need to use
 * Object.values() to convert the enum to an array first.
 *
 * In Zod v4, z.enum() was updated to accept TypeScript enums directly, but for
 * backwards compatibility, we should use Object.values() which works with both.
 *
 * See PR #1613: https://github.com/browserbase/stagehand/pull/1613
 */
(0, vitest_1.describe)("SupportedUnderstudyAction enum Zod compatibility", () => {
    const testInput = {
        elementId: "1-2",
        method: "click",
        arguments: [],
    };
    const invalidInput = {
        elementId: "1-2",
        method: "invalidMethod",
        arguments: [],
    };
    (0, vitest_1.it)("Object.values(SupportedUnderstudyAction) produces correct array for z.enum()", () => {
        const enumValues = Object.values(handlers_js_1.SupportedUnderstudyAction);
        (0, vitest_1.expect)(enumValues).toContain("click");
        (0, vitest_1.expect)(enumValues).toContain("fill");
        (0, vitest_1.expect)(enumValues).toContain("type");
        (0, vitest_1.expect)(enumValues).toContain("press");
        (0, vitest_1.expect)(enumValues).toContain("scrollTo");
        (0, vitest_1.expect)(enumValues).toContain("nextChunk");
        (0, vitest_1.expect)(enumValues).toContain("prevChunk");
        (0, vitest_1.expect)(enumValues).toContain("selectOptionFromDropdown");
        (0, vitest_1.expect)(enumValues).toContain("hover");
        (0, vitest_1.expect)(enumValues).toContain("doubleClick");
        (0, vitest_1.expect)(enumValues).toContain("dragAndDrop");
        (0, vitest_1.expect)(enumValues.length).toBe(11);
    });
    (0, vitest_1.it)("Zod v3 z.enum() with Object.values(SupportedUnderstudyAction) works correctly", () => {
        const enumValues = Object.values(handlers_js_1.SupportedUnderstudyAction);
        const schema = z3.z.object({
            elementId: z3.z.string(),
            method: z3.z.enum(enumValues),
            arguments: z3.z.array(z3.z.string()),
        });
        // Valid input should pass
        const validResult = schema.safeParse(testInput);
        (0, vitest_1.expect)(validResult.success).toBe(true);
        if (validResult.success) {
            (0, vitest_1.expect)(validResult.data.method).toBe("click");
        }
        // Invalid input should fail
        const invalidResult = schema.safeParse(invalidInput);
        (0, vitest_1.expect)(invalidResult.success).toBe(false);
    });
    (0, vitest_1.it)("Zod v4 z.enum() with Object.values(SupportedUnderstudyAction) works correctly", () => {
        const enumValues = Object.values(handlers_js_1.SupportedUnderstudyAction);
        const schema = zod_1.z.object({
            elementId: zod_1.z.string(),
            method: zod_1.z.enum(enumValues),
            arguments: zod_1.z.array(zod_1.z.string()),
        });
        // Valid input should pass
        const validResult = schema.safeParse(testInput);
        (0, vitest_1.expect)(validResult.success).toBe(true);
        if (validResult.success) {
            (0, vitest_1.expect)(validResult.data.method).toBe("click");
        }
        // Invalid input should fail
        const invalidResult = schema.safeParse(invalidInput);
        (0, vitest_1.expect)(invalidResult.success).toBe(false);
    });
    (0, vitest_1.it)("Zod v3 z.enum() with raw TypeScript enum throws error on parse", () => {
        // This demonstrates the bug that PR #1613 would introduce
        // In Zod v3, z.enum() does NOT accept TypeScript enums directly
        // The schema creation might succeed, but parsing will fail
        const schema = z3.z.object({
            elementId: z3.z.string(),
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            method: z3.z.enum(handlers_js_1.SupportedUnderstudyAction),
            arguments: z3.z.array(z3.z.string()),
        });
        // This should throw an error because the enum is not iterable
        (0, vitest_1.expect)(() => schema.safeParse(testInput)).toThrow("object is not iterable");
    });
    (0, vitest_1.it)("Zod v4 z.enum() with raw TypeScript enum works (but not v3 compatible)", () => {
        // Zod v4 allows passing TypeScript enums directly to z.enum()
        // But this approach is NOT backwards compatible with v3
        const schema = zod_1.z.object({
            elementId: zod_1.z.string(),
            method: zod_1.z.enum(handlers_js_1.SupportedUnderstudyAction),
            arguments: zod_1.z.array(zod_1.z.string()),
        });
        // In v4, this works fine
        const validResult = schema.safeParse(testInput);
        (0, vitest_1.expect)(validResult.success).toBe(true);
    });
    (0, vitest_1.it)("All SupportedUnderstudyAction values are valid enum options", () => {
        const enumValues = Object.values(handlers_js_1.SupportedUnderstudyAction);
        // Test with both v3 and v4 schemas
        const v3Schema = z3.z.enum(enumValues);
        const v4Schema = zod_1.z.enum(enumValues);
        for (const action of enumValues) {
            (0, vitest_1.expect)(v3Schema.safeParse(action).success).toBe(true);
            (0, vitest_1.expect)(v4Schema.safeParse(action).success).toBe(true);
        }
    });
});
//# sourceMappingURL=zod-enum-compatibility.test.js.map