import type { V3 } from "../../v3.js";
import type { ScrollToolResult, ScrollVisionToolResult } from "../../types/public/agent.js";
/**
 * Simple scroll tool for DOM mode (non-grounding models).
 * No coordinates - scrolls from viewport center.
 */
export declare const scrollTool: (v3: V3) => import("ai").Tool<{
    direction: "up" | "down";
    percentage?: number;
}, ScrollToolResult>;
/**
 * Scroll tool for hybrid mode (grounding models).
 * Supports optional coordinates for scrolling within nested scrollable elements.
 */
export declare const scrollVisionTool: (v3: V3, provider?: string) => import("ai").Tool<{
    direction: "up" | "down";
    coordinates?: number[];
    percentage?: number;
}, ScrollVisionToolResult>;
