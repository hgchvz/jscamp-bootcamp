import { StagehandInvalidArgumentError } from "../types/public/sdkErrors.js";
import { screenshotScriptSources } from "../dom/build/screenshotScripts.generated.js";
export function collectFramesForScreenshot(page) {
    const seen = new Map();
    const main = page.mainFrame();
    seen.set(main.frameId, main);
    for (const frame of page.frames()) {
        seen.set(frame.frameId, frame);
    }
    return Array.from(seen.values());
}
export function normalizeScreenshotClip(clip) {
    const x = Number(clip.x);
    const y = Number(clip.y);
    const width = Number(clip.width);
    const height = Number(clip.height);
    for (const [key, value] of Object.entries({ x, y, width, height })) {
        if (!Number.isFinite(value)) {
            throw new StagehandInvalidArgumentError(`screenshot: clip.${key} must be a finite number`);
        }
    }
    if (width <= 0 || height <= 0) {
        throw new StagehandInvalidArgumentError("screenshot: clip width/height must be positive");
    }
    return { x, y, width, height };
}
export async function computeScreenshotScale(page, mode) {
    if (mode !== "css")
        return undefined;
    try {
        const frame = page.mainFrame();
        const dpr = await frame
            .evaluate(() => {
            const ratio = Number(window.devicePixelRatio || 1);
            return Number.isFinite(ratio) && ratio > 0 ? ratio : 1;
        })
            .catch(() => 1);
        const safeRatio = Number.isFinite(dpr) && dpr > 0 ? dpr : 1;
        return Math.min(2, Math.max(0.1, 1 / safeRatio));
    }
    catch {
        return 1;
    }
}
export async function setTransparentBackground(session) {
    await session
        .send("Emulation.setDefaultBackgroundColorOverride", {
        color: { r: 0, g: 0, b: 0, a: 0 },
    })
        .catch(() => { });
    return async () => {
        await session
            .send("Emulation.setDefaultBackgroundColorOverride", {})
            .catch(() => { });
    };
}
export async function applyStyleToFrames(frames, css, label) {
    const trimmed = css.trim();
    if (!trimmed)
        return async () => { };
    const token = `__v3_style_${label}_${Date.now()}_${Math.random()
        .toString(36)
        .slice(2)}`;
    await Promise.all(frames.map((frame) => frame
        .evaluate(({ css, token }) => {
        try {
            const doc = document;
            if (!doc)
                return;
            const style = doc.createElement("style");
            style.setAttribute("data-stagehand-style", token);
            style.textContent = css;
            const parent = doc.head || doc.documentElement || doc.body;
            parent?.appendChild(style);
        }
        catch {
            // ignore
        }
    }, { css: trimmed, token })
        .catch(() => { })));
    return async () => {
        await Promise.all(frames.map((frame) => frame
            .evaluate((token) => {
            try {
                const doc = document;
                if (!doc)
                    return;
                const nodes = doc.querySelectorAll(`[data-stagehand-style="${token}"]`);
                nodes.forEach((node) => node.remove());
            }
            catch {
                // ignore
            }
        }, token)
            .catch(() => { })));
    };
}
export async function disableAnimations(frames) {
    const css = `
*,
*::before,
*::after {
  animation-delay: 0s !important;
  animation-duration: 0s !important;
  animation-iteration-count: 1 !important;
  animation-play-state: paused !important;
  transition-property: none !important;
  transition-duration: 0s !important;
  transition-delay: 0s !important;
}`;
    const cleanup = await applyStyleToFrames(frames, css, "animations");
    await Promise.all(frames.map((frame) => frame
        .evaluate(() => {
        try {
            const animations = typeof document.getAnimations === "function"
                ? document.getAnimations()
                : [];
            for (const animation of animations) {
                try {
                    const details = animation.effect?.getComputedTiming?.();
                    if (details && details.iterations !== Infinity) {
                        animation.finish?.();
                    }
                    else {
                        animation.cancel?.();
                    }
                }
                catch {
                    animation.cancel?.();
                }
            }
        }
        catch {
            // ignore
        }
    })
        .catch(() => { })));
    return cleanup;
}
export async function hideCaret(frames) {
    const css = `
input,
textarea,
[contenteditable],
[contenteditable=""],
[contenteditable="true"],
[contenteditable="plaintext-only"],
*:focus {
  caret-color: transparent !important;
}`;
    return applyStyleToFrames(frames, css, "caret");
}
export async function applyMaskOverlays(locators, color) {
    const rectsByFrame = new Map();
    const token = `__v3_mask_${Date.now()}_${Math.random().toString(36).slice(2)}`;
    for (const locator of locators) {
        try {
            const info = await resolveMaskRects(locator, token);
            if (!info)
                continue;
            const entry = rectsByFrame.get(info.frame) ?? {
                rects: [],
                rootTokens: new Set(),
            };
            entry.rects.push(...info.rects);
            for (const rect of info.rects) {
                if (rect.rootToken)
                    entry.rootTokens.add(rect.rootToken);
            }
            rectsByFrame.set(info.frame, entry);
        }
        catch {
            // ignore individual locator failures
        }
    }
    if (rectsByFrame.size === 0) {
        return async () => { };
    }
    await Promise.all(Array.from(rectsByFrame.entries()).map(([frame, { rects }]) => frame
        .evaluate(({ rects, color, token }) => {
        try {
            const doc = document;
            if (!doc)
                return;
            for (const rect of rects) {
                const defaultRoot = doc.documentElement || doc.body;
                if (!defaultRoot)
                    return;
                const root = rect.rootToken
                    ? doc.querySelector(`[data-stagehand-mask-root="${rect.rootToken}"]`) || defaultRoot
                    : defaultRoot;
                if (!root)
                    continue;
                if (rect.rootToken) {
                    try {
                        const style = window.getComputedStyle(root);
                        if (style && style.position === "static") {
                            const rootEl = root;
                            if (!rootEl.hasAttribute("data-stagehand-mask-root-pos")) {
                                rootEl.setAttribute("data-stagehand-mask-root-pos", rootEl.style.position || "");
                            }
                            rootEl.style.position = "relative";
                        }
                    }
                    catch {
                        // ignore
                    }
                }
                const el = doc.createElement("div");
                el.setAttribute("data-stagehand-mask", token);
                el.style.position = "absolute";
                el.style.left = `${rect.x}px`;
                el.style.top = `${rect.y}px`;
                el.style.width = `${rect.width}px`;
                el.style.height = `${rect.height}px`;
                el.style.backgroundColor = color;
                el.style.pointerEvents = "none";
                el.style.zIndex = "2147483647";
                el.style.opacity = "1";
                el.style.mixBlendMode = "normal";
                root.appendChild(el);
            }
        }
        catch {
            // ignore
        }
    }, { rects, color, token })
        .catch(() => { })));
    return async () => {
        await Promise.all(Array.from(rectsByFrame.entries()).map(([frame, { rootTokens }]) => frame
            .evaluate(({ token, rootTokens }) => {
            try {
                const doc = document;
                if (!doc)
                    return;
                const nodes = doc.querySelectorAll(`[data-stagehand-mask="${token}"]`);
                nodes.forEach((node) => node.remove());
                for (const rootToken of rootTokens) {
                    const root = doc.querySelector(`[data-stagehand-mask-root="${rootToken}"]`);
                    if (!root)
                        continue;
                    const prev = root.getAttribute("data-stagehand-mask-root-pos");
                    if (prev !== null) {
                        root.style.position = prev;
                        root.removeAttribute("data-stagehand-mask-root-pos");
                    }
                    root.removeAttribute("data-stagehand-mask-root");
                }
            }
            catch {
                // ignore
            }
        }, { token, rootTokens: Array.from(rootTokens) })
            .catch(() => { })));
    };
}
async function resolveMaskRects(locator, maskToken) {
    const frame = locator.getFrame();
    const session = frame.session;
    try {
        const resolved = await locator.resolveNodesForMask();
        const rects = [];
        for (const { objectId } of resolved) {
            try {
                const rect = await resolveMaskRectForObject(session, objectId, maskToken);
                if (rect)
                    rects.push(rect);
            }
            catch {
                // ignore individual element failures
            }
            finally {
                await session
                    .send("Runtime.releaseObject", { objectId })
                    .catch(() => { });
            }
        }
        if (!rects.length)
            return null;
        return { frame, rects };
    }
    catch {
        return null;
    }
}
async function resolveMaskRectForObject(session, objectId, maskToken) {
    const result = await session.send("Runtime.callFunctionOn", {
        objectId,
        functionDeclaration: screenshotScriptSources.resolveMaskRect,
        arguments: [{ value: maskToken }],
        returnByValue: true,
    });
    if (result.exceptionDetails) {
        return null;
    }
    const rect = result.result.value;
    if (!rect)
        return null;
    const { x, y, width, height } = rect;
    if (!Number.isFinite(x) ||
        !Number.isFinite(y) ||
        !Number.isFinite(width) ||
        !Number.isFinite(height) ||
        width <= 0 ||
        height <= 0) {
        return null;
    }
    return {
        x,
        y,
        width,
        height,
        rootToken: rect.rootToken && typeof rect.rootToken === "string"
            ? rect.rootToken
            : undefined,
    };
}
export async function runScreenshotCleanups(cleanups) {
    for (let i = cleanups.length - 1; i >= 0; i -= 1) {
        const fn = cleanups[i];
        if (!fn)
            continue;
        try {
            const result = fn();
            if (result && typeof result.then === "function") {
                await result;
            }
        }
        catch {
            // ignore cleanup errors
        }
    }
}
//# sourceMappingURL=screenshotUtils.js.map